-- ATAT Application Tracking Schema
-- Phase 2 — not yet implemented

-- applications table will track:
--   id, company, role, persona, tier, jd_path, cv_path,
--   status (generated | reviewed | submitted | acknowledged | rejected | offer),
--   source_url, applied_at, created_at, updated_at, notes

-- placeholder for future migration tooling
